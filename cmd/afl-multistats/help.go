package main

const (
	helpText = `afl-multistats [options] /path/to/sync/dir
	
Options:

	-format fmt   - the format to output stats in (json)
	-advanced     - output all stats, not just summary
	-hud          - display a persistent stats HUD	
`
)
